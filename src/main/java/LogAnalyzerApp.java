import configurations.ConfigurationRepository;
import emailrepository.EmailRepository;
import input.Input;
import output.Output;

public class LogAnalyzerApp {
    public void runApp(Input input, Output output, ConfigurationRepository configurationRepository, EmailRepository emailRepository){
        while(true){

        }
    }
}
